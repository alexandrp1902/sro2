// Раскладка у станции — в её осях (sim/orbits.ts): +y — прочь от звезды. Точка спауна совпадает с сервером.

export const SPAWN = { x: 0, y: 420 };
export const STATION = { x: 0, y: 0, radius: 90 };
