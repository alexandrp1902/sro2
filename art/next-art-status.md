# Статус генерации по next-art-requests.md

Задание выполнено частично: сохранены 27 из 153 PNG из существующего манифеста. Генерация остановлена ответом встроенного image_gen: HTTP 429, usage_limit_reached. Указанное сервисом время сброса: 20 сентября 2026, 02:59:55 МСК (19 сентября 23:59:55 UTC). Автоматическое продолжение не запланировано.

## Сохранённые исходники

- Пачка A: 8 кораблей, `space/npc-ships/`.
- Пачка A2: 4 листа эффектов оружия, `space/weapon-effects/`.
- Пачка B: 6 планет, 4 станции, 2 звезды, 3 фона, `space/galaxy/`.

Использован встроенный image_gen, отдельный вызов для каждого изображения. Промпты находятся в `prompts.md` соответствующих папок. Точные отправленные строки и пути оригинальных результатов сохранены в `next-art-generation-log.json`. Существующие промпты остальных пачек являются подготовленными заданиями, а не подтверждением выполненной генерации.

## Проверка и оставшиеся отклонения

`node art/audit-next-art.mjs` обновляет `next-art-audit.json`: 27 читаемых PNG, 126 отсутствующих, ошибок декодирования нет. Все созданные спрайты и эффекты имеют прозрачные пиксели.

Это исходники генератора, ещё не полностью соответствующие техническому заданию:

- Размеры всех 27 PNG отличаются от указанных в манифесте. Квадратные исходники имеют размер 1254×1254; требуемые размеры записаны рядом с фактическими в аудите.
- У кораблей и некоторых станций отступы меньше требуемых 6–8%; отдельные выступы близки к краям. Перед приёмкой необходима коррекция композиции.
- На краях планет и колец визуально заметны неровности и остаточные светлые/цветные пиксели; необходима очистка или повторная генерация.
- Бесшовность трёх фонов не подтверждена. Листы эффектов требуют проверки формы снарядов и точности расположения в ячейках.

Нарезка, конвертация и подключение в игру не выполнялись: по заданию пользователь делает это сам.

## Продолжение

Первый несозданный файл: `space/trade-missions/goods-food.png` (индекс 27, считая с нуля, в `next-art-manifest.json`). Продолжить пачки C, D, E — 126 файлов — после восстановления лимита. Затем исправить перечисленные отклонения и повторить проверку всего набора. Уже сохранённые исходники при исправлениях сохранять, новые варианты создавать отдельными файлами.

## Что подключено в игру (M11, 2026-09-20)

Нарезаны в `client/public/sprites/` скриптом `tools/sprites.py`: восемь кораблей NPC пачки A, четыре листа эффектов оружия (A2), шесть планет, четыре станции и две звезды пачки B, а также ранее сохранённые шесть корпусов (`ships-extra`) и двенадцать иконок снаряжения (`equipment-extra`).

**Временные замены — их надо снять, когда придёт свой арт:**

1. **Пламя двигателей** у шести новых корпусов игрока берётся от старых кораблей по размеру: light-flame — «Стриж», «Сокол», «Мул»; medium-flame — «Страж», «Караван»; heavy-flame — «Титан». Код: `FLAME_STANDINS` в `client/src/render/sprites.ts`.
2. **Фоны дока** на новых станциях — существующие сцены станции (офис, торговец, верфь, ангар).
3. **Станции Рубежа** нарисованы имеющимися спрайтами: `fortress` (Nova), `outpost` (Альдебаран), `habitat` (Эпсилон), `pirate` — у логова Края.
4. **`ships-ranger-heavy` и `ships-trader-convoy`** нарезаны, но в M11 не используются: они нужны для заданий M14.
5. **Фоны регионов** `bg-core`, `bg-frontier`, `bg-rim` не подключены: небо рисуется процедурной туманностью.
6. Исходники пачек A, A2 и B приняты как есть, с отмеченными в аудите недочётами (отступы, края). Перегенерация заменит файлы без правок кода.
